name: SimVP
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - numpy
  - pip
  - python
  - pytorch
  - pip:
    - tqdm
    - scikit-image
    - opencv-python
    - matplotlib
prefix: /opt/anaconda3/envs/simvp